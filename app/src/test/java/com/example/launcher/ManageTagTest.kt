package com.example.launcher

import org.junit.Test

class ManageTagTest {
    @Test
    fun testReorderLogic() {
        // This is primarily for defining the logic of reordering in the UI
        // before we implement it.
    }
}
